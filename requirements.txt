# Core Django and Web
django>=4.0,<5.0
requests>=2.25.0,<3.0.0
Pillow>=9.0.0,<11.0.0 # For Django image handling, if needed later

# Streamlit
streamlit==1.40.1

# Machine Learning and Computer Vision
tensorflow>=2.10.0,<2.13.0 # Choose a version compatible with your anandfinal.hdf5 model
numpy==1.23.5 # TensorFlow 2.10-2.12 typically works well with numpy 1.23.x
opencv-python>=4.6.0,<5.0.0
deepface>=0.0.75,<0.0.80
h5py>=3.7.0,<3.11.0 # For loading .hdf5 models, compatible with TF and numpy

# Added to resolve trio dependencies
cffi>=1.14 # For trio on Windows
sortedcontainers # For trio

# Added/Updated to resolve optree dependency
typing-extensions>=4.6.0 # Ensure version is high enough for optree

# Optional, from Face-Liveness-Detection (if its specific utilities are still used directly)
# imutils>=0.5.4
# scikit-learn>=1.0.0,<1.4.0

# Note: Keras is part of TensorFlow (tensorflow.keras)
